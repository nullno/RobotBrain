apply plugin: 'com.android.library'

android {
    namespace 'com.nullno.robot.androidlib'
    compileSdkVersion 33
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 33
    }
    // No explicit sourceSets needed as we are using standard src/main structure now
}
